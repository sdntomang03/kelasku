
import IntegrityModal from '../components/common/IntegrityModal'

export default function TokenPage({ exam, token, setToken, error, loading, onBack, onSubmit, integrityOpen, integritySeconds, onIntegrityConfirm }) {
  return <div className="token-page"><div className="token-card"><div className="brand"><span className="brand-mark">C</span><span>kelas<span className="brand-accent">ku</span></span></div><span className="eyebrow">VERIFIKASI UJIAN</span><h1>Masukkan token ujian</h1><p className="muted">Ujian <strong>{exam?.title || 'ini'}</strong> memerlukan token sebelum dapat dimulai.</p><form onSubmit={(event) => { event.preventDefault(); onSubmit() }}><label htmlFor="exam-token">Token ujian</label><input id="exam-token" value={token} onChange={(event) => setToken(event.target.value)} placeholder="Masukkan token" autoComplete="off" autoFocus disabled={loading || integrityOpen} />{error && <div className="field-error">{error}</div>}<div className="token-actions"><button type="button" className="secondary-button" onClick={onBack} disabled={integrityOpen}>Kembali</button><button type="submit" className="primary-button" disabled={loading || integrityOpen}>{loading ? 'Memproses...' : 'Mulai ujian'}</button></div></form></div>{integrityOpen && <IntegrityModal seconds={integritySeconds} onConfirm={onIntegrityConfirm} />}</div>
}
