
function Logo() {
  return <div className="brand"><span className="brand-mark">C</span><span>kelas<span className="brand-accent">ku</span></span></div>
}

export default function LoginPage({ login, setLogin, onSubmit, error, loading }) {
  return <div className="login-page"><div className="login-decoration decor-one" /><div className="login-decoration decor-two" /><div className="login-panel"><Logo /><div className="login-copy"><span className="eyebrow">PORTAL SISWA</span><h1>Belajar lebih terarah,<br /><em>berprestasi lebih hebat.</em></h1><p>Tempat kamu mengerjakan ujian, melihat hasil, dan melacak perkembangan belajar.</p></div><div className="login-quote"><span>“</span><p>Kesuksesan adalah hasil dari persiapan, kerja keras, dan belajar dari kegagalan.</p></div></div><div className="login-form-wrap"><div className="login-form"><div className="mobile-login-logo"><Logo /></div><span className="eyebrow">MASUK KE AKUN</span><h2>Selamat datang kembali</h2><form onSubmit={onSubmit}><label htmlFor="username">Username atau NIS</label><input id="username" value={login.username} onChange={(event) => setLogin((current) => ({ ...current, username: event.target.value }))} placeholder="Masukkan username" autoComplete="username" disabled={loading} /><label htmlFor="password">Password</label><input id="password" type="password" value={login.password} onChange={(event) => setLogin((current) => ({ ...current, password: event.target.value }))} placeholder="Masukkan password" autoComplete="current-password" disabled={loading} />{error && <div className="field-error">{error}</div>}<button type="submit" className="primary-button wide" disabled={loading}>{loading ? 'Memproses...' : 'Masuk'}</button></form><div className="login-footer"><span>Butuh bantuan?</span><button className="text-button">Hubungi admin</button></div></div></div></div>
}
